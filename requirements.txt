# Install into a local virtualenv:
#   python3 -m venv venv
#   ./venv/bin/pip install -r requirements.txt
# Tested with: numpy 2.5.2, uproot 5.7.6, awkward 2.13.0, PyYAML 6.0.3,
#              pytest 9.1.1, matplotlib 3.11.1 (python 3.13, macOS)
numpy
uproot>=5.5
awkward>=2.0
pyyaml
pytest
matplotlib

# Optional -- only for reweight.py --stl-vectors, which writes the dials as
# genuine std::vector<double> branches (what PROfit's SetBranchAddress binds
# to). PyROOT is NOT pip-installable; it ships with ROOT itself:
#   brew install root          (or: conda install -c conda-forge root)
#   export PYTHONPATH=$(root-config --libdir)
# Check:  ./venv/bin/python -c "import ROOT; print(ROOT.gROOT.GetVersion())"
# Tested with ROOT 6.36.00. Without it the default uproot writer is unaffected.
